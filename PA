#include <iostream>
int main() {
  unsigned long long int number;
  std::cin >> number;
  unsigned long long int reverse_number = 0;
  for (number; number >= 1; number = number / 10) {
    reverse_number = (reverse_number * 10) + (number % 10);
  }
  std::cout << reverse_number;
}
